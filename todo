--create a subroutine for ascii conversion and store to CMDA (perhaps automatically reads and outputs it if we don't already have a routine for that)
--check RPM calc
--check ramp code (need stephen's help for which reg is what)
